---
name: 🐛 Bug report
about: Something misbehaves
labels: bug
---

## What happened

<!-- What you did, what you expected, what happened instead. -->

## Steps to reproduce

1.
2.
3.

## Environment

- OS and version:
- Herdr version (`herdr --version`):
- herdr-fingers version (`herdr plugin list`):
- Terminal (and whether you attach over SSH):
- Keyboard layout (`keyboard_layout` / `alphabet` in config.toml):

## Extra context

<!-- A screen dump helps a lot: `herdr pane read <pane-id> --source visible --format ansi > dump.txt`,
     then `herdr-fingers scan --width <pane width> < dump.txt`. Also the tail of
     $HERDR_PLUGIN_STATE_DIR/herdr-fingers.log if the overlay closed unexpectedly. -->
